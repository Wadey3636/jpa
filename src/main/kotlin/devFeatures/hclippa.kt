package me.jpaMain.devFeatures
import me.jpaMain.jpaMain.mc
//import com.github.Wadey.config.jpaConfig.hClip
import kotlin.math.cos
import kotlin.math.sin


fun hclippa(){
        /*
        if (!hClip) return;
        mc.thePlayer.setVelocity(0.0, mc.thePlayer.motionY, 0.0);
        val CameraYaw = mc.thePlayer.rotationYaw * Math.PI / 180;
        //UChat.chat(mc.thePlayer.cameraYaw);
        val y = mc.thePlayer.rotationPitch / 80;
        val x = -sin(CameraYaw) * 1.4;
        val z = cos(CameraYaw) * 1.4;
        mc.thePlayer.setVelocity(x, mc.thePlayer.motionY - y, z);
*/

    }
